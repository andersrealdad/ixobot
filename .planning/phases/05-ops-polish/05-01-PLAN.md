---
phase: 05-ops-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nanobot/skills/arena-builder/scripts/arena_run.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03]

must_haves:
  truths:
    - "harvest_insights.py is invoked automatically after the second competitor finishes an arena run"
    - "REQUIREMENTS.md traceability Status column matches checkbox state for all requirements"
    - "NAME-01 shows Complete in both checkbox and traceability"
  artifacts:
    - path: "nanobot/skills/arena-builder/scripts/arena_run.py"
      provides: "Auto-trigger for harvester after both competitors finish"
      contains: "harvest_insights.py"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected traceability table"
      contains: "Complete"
  key_links:
    - from: "nanobot/skills/arena-builder/scripts/arena_run.py"
      to: "nanobot/skills/arena-builder/scripts/harvest_insights.py"
      via: "subprocess call after post_result detects both competitors done"
      pattern: "harvest_insights"
---

<objective>
Operational polish: wire automatic harvester trigger and fix traceability table.

Purpose: Close the last three gap-closure requirements (OPS-01/02/03) so the arena pipeline runs end-to-end without manual intervention and REQUIREMENTS.md accurately reflects project state.
Output: Modified arena_run.py with harvester auto-trigger, corrected REQUIREMENTS.md traceability table.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ops-polish/05-CONTEXT.md
@nanobot/skills/arena-builder/scripts/arena_run.py
@nanobot/skills/arena-builder/scripts/harvest_insights.py

<interfaces>
From nanobot/skills/arena-builder/scripts/arena_run.py:
```python
def post_result(agent_name: str, bo_id: str, model: str, status: str, insight_count: int, duration_s: float) -> None:
    """Post arena result to message_bus in shared-memory.db."""
```
Constants:
```python
SHARED_DATA = Path.home() / "shared-data"
SHARED_MEMORY_DB = SHARED_DATA / "db" / "shared-memory.db"
```

From nanobot/skills/arena-builder/scripts/harvest_insights.py:
```python
# Invoked as: python3 harvest_insights.py --bo-id <BO_ID>
# Auto-detect mode (no args) also works but --bo-id is more precise
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add harvester auto-trigger to arena_run.py post_result flow</name>
  <read_first>
    - nanobot/skills/arena-builder/scripts/arena_run.py (full file, especially post_result and main)
    - nanobot/skills/arena-builder/scripts/harvest_insights.py (lines 1-20 for usage, lines 69-102 for find_completed_bos)
  </read_first>
  <files>nanobot/skills/arena-builder/scripts/arena_run.py</files>
  <action>
    Add a new function `trigger_harvester(bo_id: str)` to arena_run.py that:

    1. Queries message_bus in shared-memory.db for rows where channel='arena-build' and metadata contains the bo_id
    2. Counts how many result rows exist for that bo_id (parse metadata JSON, match bo_id)
    3. If count >= 2 (both competitors done), invokes harvest_insights.py via subprocess:
       ```python
       HARVEST_SCRIPT = Path(__file__).parent / "harvest_insights.py"
       subprocess.Popen(
           [sys.executable, str(HARVEST_SCRIPT), "--bo-id", bo_id],
           stdout=subprocess.DEVNULL,
           stderr=subprocess.DEVNULL,
       )
       ```
    4. Uses Popen (not run) so it does not block the arena_run exit
    5. Prints "Arena: harvester triggered for BO #{bo_id}" on trigger, or "Arena: waiting for other competitor (N/2 done)" if not yet ready

    Call `trigger_harvester(bo_id)` in main() right after `post_result(...)` call (around line 374), ONLY when not in dry-run mode.

    Per OPS-01: this makes the harvester fire automatically after arena runs complete.

    IMPORTANT: Do NOT change the function signature of post_result. Add trigger_harvester as a separate function.
    IMPORTANT: The count query must parse metadata JSON from message_bus rows, same pattern as harvest_insights.py find_completed_bos().
    IMPORTANT: Only count rows where metadata type is NOT "harvest_summary" (to avoid counting the harvester's own summary post).
  </action>
  <verify>
    <automated>cd /home/superfuru/DEV/ixosynth && python3 -c "import ast; tree = ast.parse(open('nanobot/skills/arena-builder/scripts/arena_run.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'trigger_harvester' in funcs, f'Missing trigger_harvester, found: {funcs}'" && grep -q "harvest_insights" nanobot/skills/arena-builder/scripts/arena_run.py && grep -q "trigger_harvester" nanobot/skills/arena-builder/scripts/arena_run.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "def trigger_harvester" arena_run.py returns 1 match
    - grep "harvest_insights" arena_run.py returns at least 1 match
    - grep "Popen" arena_run.py returns at least 1 match
    - grep "trigger_harvester(bo_id)" arena_run.py shows call in main flow
    - grep "dry_run" near trigger call shows it is gated on not dry_run
    - python3 -c "import ast; ast.parse(open('...arena_run.py').read())" exits 0 (valid syntax)
  </acceptance_criteria>
  <done>trigger_harvester function exists in arena_run.py, is called after post_result in main, checks message_bus for 2 completed results before spawning harvest_insights.py as a non-blocking subprocess</done>
</task>

<task type="auto">
  <name>Task 2: Fix REQUIREMENTS.md traceability table to match checkbox state</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update the traceability table (bottom of REQUIREMENTS.md) to match actual checkbox state:

    Per OPS-02 and OPS-03:
    1. Change PROV-01 status from "Pending" to "Complete" (checkbox is [x] on line 35)
    2. Change PROV-02 status from "Pending" to "Complete" (checkbox is [x] on line 36)
    3. Change PROV-03 status from "Pending" to "Complete" (checkbox is [x] on line 37)
    4. Change OPS-01 status from "Pending" to "Complete" (will be done by Task 1)
    5. Change OPS-02 status from "Pending" to "Complete" (this task fixes it)
    6. Change OPS-03 status from "Pending" to "Complete" (NAME-01 already shows Complete in traceability -- OPS-03 just verifies this, which it does)
    7. Mark OPS-01, OPS-02, OPS-03 checkboxes as [x] in the requirements list (lines 41-43)
    8. Update the Coverage section: change "Gap closure requirements: 6 total (pending)" to "Gap closure requirements: 6 total (all complete)"

    NAME-01 already shows "Complete" in the traceability table (line 83), so OPS-03 is satisfied.

    Do NOT change any other content. Only update Status column values and checkboxes.
  </action>
  <verify>
    <automated>cd /home/superfuru/DEV/ixosynth && grep -c "Pending" .planning/REQUIREMENTS.md | grep -q "^0$" && echo "PASS: no Pending rows" || echo "FAIL: still has Pending rows"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Pending" .planning/REQUIREMENTS.md returns 0 matches
    - grep -c "Complete" in traceability table returns 19 (all requirements)
    - grep "\[x\] \*\*OPS-01\*\*" .planning/REQUIREMENTS.md returns 1 match
    - grep "\[x\] \*\*OPS-02\*\*" .planning/REQUIREMENTS.md returns 1 match
    - grep "\[x\] \*\*OPS-03\*\*" .planning/REQUIREMENTS.md returns 1 match
    - grep "all complete" .planning/REQUIREMENTS.md returns 1 match
  </acceptance_criteria>
  <done>All 19 requirement rows show "Complete" in traceability, all checkboxes are [x], coverage section updated</done>
</task>

</tasks>

<verification>
1. `python3 -c "import ast; ast.parse(open('nanobot/skills/arena-builder/scripts/arena_run.py').read())"` -- valid Python
2. `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0
3. `grep "trigger_harvester" nanobot/skills/arena-builder/scripts/arena_run.py` shows function and call site
4. `grep "harvest_insights" nanobot/skills/arena-builder/scripts/arena_run.py` confirms script reference
</verification>

<success_criteria>
- arena_run.py automatically triggers harvest_insights.py when both competitors have posted results (OPS-01)
- REQUIREMENTS.md traceability table has zero "Pending" entries, all show "Complete" (OPS-02)
- NAME-01 confirmed Complete in traceability (OPS-03)
- No syntax errors in modified Python file
</success_criteria>

<output>
After completion, create `.planning/phases/05-ops-polish/05-01-SUMMARY.md`
</output>
