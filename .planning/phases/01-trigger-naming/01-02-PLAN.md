---
phase: 01-trigger-naming
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/superfuru/DEV/ixosynth/.git/config
autonomous: false
requirements: [NAME-01]

must_haves:
  truths:
    - "Git remotes use a consistent project name across GitHub and Gitea"
    - "Local directory path remains ~/DEV/ixosynth/ (no directory rename needed)"
  artifacts:
    - path: "/home/superfuru/DEV/ixosynth/.git/config"
      provides: "Git remote configuration with aligned naming"
      contains: "ixobot"
  key_links: []
---

<objective>
Align the naming between GitHub repo (andersrealdad/ixobot), Gitea repo (superfuru/IXO-Synth), and the local directory (~/DEV/ixosynth/).

Purpose: Reduce confusion when referencing the project across remotes. The local directory stays as-is (ixosynth), but the remote names and references should be documented and consistent.
Output: Updated git remote config + decision documented
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/superfuru/DEV/ixosynth/.planning/PROJECT.md
@/home/superfuru/DEV/ixosynth/.planning/ROADMAP.md
@/home/superfuru/DEV/ixosynth/.planning/STATE.md

<interfaces>
Current git remotes (from `git remote -v`):
```
github  git@github.com:andersrealdad/ixobot.git (fetch/push)
origin  git@gitea:superfuru/IXO-Synth.git (fetch/push)
```

Local directory: ~/DEV/ixosynth/
GitHub repo name: ixobot
Gitea repo name: IXO-Synth
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide naming alignment strategy</name>
  <files>/home/superfuru/DEV/ixosynth/.git/config</files>
  <read_first>
    /home/superfuru/DEV/ixosynth/.git/config
  </read_first>
  <action>
Present the user with three options for aligning the naming across GitHub (andersrealdad/ixobot), Gitea (superfuru/IXO-Synth), and local dir (~/DEV/ixosynth/):

- **option-a**: Rename GitHub repo to ixosynth (align to local + product name). Matches local dir, product name. GitHub auto-redirects old URL.
- **option-b**: Rename Gitea repo to ixobot (align to GitHub name). Both remotes match. Misaligns with product name IxoSynth.
- **option-c**: Keep all three names, document the mapping. Zero breakage risk, just documentation.

The product name is "IxoSynth" (the synthesizer core). "IxoBot" is the agent fork (nanobot + memory). The repo contains the nanobot framework which IS IxoSynth. Local dir is referenced in CLAUDE.md, agent configs, and scripts — renaming it would break many things.

Wait for user decision before proceeding to Task 2.
  </action>
  <verify>
    <automated>echo "checkpoint:decision — requires user input"</automated>
  </verify>
  <acceptance_criteria>
    - User has selected an option (option-a, option-b, or option-c)
  </acceptance_criteria>
  <done>User has chosen a naming alignment strategy</done>
</task>

<task type="auto">
  <name>Task 2: Execute chosen naming alignment</name>
  <files>/home/superfuru/DEV/ixosynth/.git/config</files>
  <read_first>
    /home/superfuru/DEV/ixosynth/.git/config
  </read_first>
  <action>
Based on the decision from Task 1, execute the alignment:

**If option-a (rename GitHub to ixosynth):**
1. Rename GitHub repo via `gh repo rename ixosynth --repo andersrealdad/ixobot --yes`
2. Update local remote: `git remote set-url github git@github.com:andersrealdad/ixosynth.git`
3. Verify: `git remote -v` shows consistent naming

**If option-b (rename Gitea to ixobot):**
1. Rename Gitea repo via API: `curl -X PATCH "http://ubsfuru:3001/api/v1/repos/superfuru/IXO-Synth" -H "Authorization: token $(~/bin/bw-fetch-secret claude-code)" -H "Content-Type: application/json" -d '{"name":"ixobot"}'`
2. Update local remote: `git remote set-url origin git@gitea:superfuru/ixobot.git`
3. Verify: `git remote -v`

**If option-c (document only):**
1. Add a "Repo Naming" section to CLAUDE.md in the ixosynth repo documenting:
   - Local: ~/DEV/ixosynth/
   - Gitea: superfuru/IXO-Synth (origin)
   - GitHub: andersrealdad/ixobot (github)
   - Product name: IxoSynth
2. No remote changes needed

In all cases, verify with `git fetch --all --dry-run` to confirm remotes are reachable.
  </action>
  <acceptance_criteria>
    - `git remote -v` in ~/DEV/ixosynth/ shows valid, reachable remotes
    - `git fetch --all --dry-run` succeeds (remotes accessible)
    - If option-a: `git remote -v` contains "ixosynth" for github remote
    - If option-b: `git remote -v` contains "ixobot" for origin remote
    - If option-c: CLAUDE.md contains "IXO-Synth" and "ixobot" mapping documentation
  </acceptance_criteria>
  <verify>
    <automated>cd /home/superfuru/DEV/ixosynth && git remote -v && git fetch --all --dry-run 2>&1 | head -5</automated>
  </verify>
  <done>Git remotes are aligned per chosen strategy, remotes are reachable, naming decision is documented</done>
</task>

</tasks>

<verification>
1. `git remote -v` shows consistent, reachable remotes
2. `git fetch --all --dry-run` succeeds
3. Decision is recorded in STATE.md
</verification>

<success_criteria>
- Naming alignment decision made by user
- Remotes updated (or documented) per decision
- All remotes reachable after changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-trigger-naming/01-02-SUMMARY.md`
</output>
